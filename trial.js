 arr.forEach(function(item){ 
  <p>item.Cart_ID</p>
  for (var key in JSON.parse(item.Items){ 
    arr_data.forEach(function(val){
      if(val.Item_ID === key){ 
        <p>val.Item_Name </p>
        }
        })
  })
})